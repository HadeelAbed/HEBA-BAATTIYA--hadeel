# تشغيل بوابة الدفع (Tap) — دليل خطوة بخطوة

## ما الجاهز الآن
التكامل الكامل مبني على الموقع وتم رفعه على Vercel. ينقص فقط **ربط مفاتيح حسابك** على تاب حتى يبدأ قبول الدفع بالبطاقات (مدى / فيزا / ماستركارد / Apple Pay / STC Pay).

> ملاحظة أمان: المفاتيح **لا تُرفع أبداً على GitHub**. تُضاف مباشرة في لوحة Vercel فقط.

---

## 1) استخراج المفاتيح من تاب
1. ادخلي إلى لوحة تاب: https://tap.company → سجّلي الدخول.
2. من القائمة: **Developers (المطورون) → Keys (المفاتيح)**.
3. انسخي:
   - **Secret Key** (يبدأ بـ `sk_test_...` للاختبار، و`sk_live_...` للدفع الفعلي).
   - **Webhook Secret** (اختياري لكن موصى به بشدة).

> أثناء التجربة استخدمي مفاتيح الاختبار `sk_test_...` أولاً — لا تخصم أموالاً حقيقية.

---

## 2) إضافة المفاتيح على Vercel
1. افتحي [vercel.com](https://vercel.com) → مشروعك (HEBA-BAATTIYA) → **Settings** → **Environment Variables**.
2. أضيفي متغيرين (واحدا تلو الآخر):
   | الاسم | القيمة |
   |---|---|
   | `TAP_SECRET_KEY` | المفتاح من تاب (مثل `sk_test_xxxx`) |
   | `TAP_WEBHOOK_SECRET` | من تاب (اختياري) |
3. اختر البيئات: **Production** (+ **Preview** و **Development** إن أردت التجربة قبل النشر).
4. اضغطي **Save**.
5. Vercel سيعيد النشر تلقائياً بهذه المفاتيح — تأكدي من **Deployments** أن البناء نجح.

---

## 3) تسجيل رابط Webhook في تاب
1. من لوحة تاب: **Developers → Webhooks**.
2. أضيفي Webhook جديد بالرابط:
   ```
   https://your-domain.com/api/payments/webhook
   ```
   (استبدلي `your-domain.com` بنطاقك الفعلي، مثل `hebabaattiya.com`).
3. فعّلي إشعارات الدفع (charges) واحفظي.

---

## 4) التجربة
1. افتحي المتجر → أضيفي منتجاً إلى السلة → أتمّي الطلب.
2. اختاري الدفع بالبطاقة (مدى / فيزا).
3. ستُوجهين إلى صفحة دفع تاب → جربي ببطاقة اختبارية من تاب (قسم **Test Cards** في لوحة المطورين).
4. بعد الدفع تعودين لصفحة "تم تأكيد الطلب" وتظهر حالة **Payment received**.
5. تأكدي من لوحة الأدمن `/admin/orders` أن الطلب ظهر بحالة الدفع **PAID**.

> للاختبار المحلي على جهازك: ضعي نفس المفاتيح في ملف `.env` ثم شغّلي `npm run dev`.

---

## تذكير أمني 🔐
- `TAP_SECRET_KEY` و`TAP_WEBHOOK_SECRET` من أسرارك — لا تشاركيهما مع أي شخص، ولا تضعيهما في أي رسالة أو ملف مرفوع على GitHub.
- أضيفيهما **فقط** في: ملف `.env` (محلي) أو لوحة Vercel (إنتاج).
- عند الانتقال للدفع الفعلي: بدّلي المفتاح إلى `sk_live_...` في Vercel (وليس في كود الموقع).

---

## استكشاف الأخطاء
- **"Online payments are not available yet"** عند إتمام الطلب → مفتاح `TAP_SECRET_KEY` غير موجود/فارغ في Vercel. أضيفيه وأعيدي النشر.
- **الدفع تم لكن الطلب ما صار PAID** → تأكدي أن رابط الـ webhook مسجل في تاب وأن `TAP_WEBHOOK_SECRET` مطابق لما في لوحة تاب.
- **خطأ في إعدادات تاب** → من لوحة Vercel → Deployments → Latest → Logs، وافحصي رسالة الخطأ.
